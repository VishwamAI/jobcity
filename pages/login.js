import React from 'react';
import AuthComponent from '../src/components/auth-pages';

const LoginPage = () => {
  return (
    <div>
      <AuthComponent />
    </div>
  );
};

export default LoginPage;
